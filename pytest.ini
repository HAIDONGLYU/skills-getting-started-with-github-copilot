[pytest]
pythonpath = .
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: Unit tests (no HTTP)
    api: API endpoint tests with HTTP
    integration: Integration tests for multi-step workflows
